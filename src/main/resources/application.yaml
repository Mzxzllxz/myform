spring:
  application:
    name: "myform"

  datasource:
    url: jdbc:mariadb://localhost:3306/maria
    driver-class-name: org.mariadb.jdbc.Driver
    username: admin
    password: maira
    jpa:
      open-in-view: false
      generate-ddl: true
      show-sql: true
      hibernate:
        ddl-auto: updates